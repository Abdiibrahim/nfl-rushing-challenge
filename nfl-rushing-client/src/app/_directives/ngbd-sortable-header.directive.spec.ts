import { NgdbSortableHeaderDirective } from './ngdb-sortable-header.directive';

describe('NgdbSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgdbSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
